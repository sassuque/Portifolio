import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
page:{
    width: "84rem",
    height: "143rem",
    display: "flex",
    flexDirection: "column",
},
}))